{
  "app_launcher": {
    "description": "Open an app installed on this computer. Use when the user asks to open or start a program, e.g. 'open notepad' or 'start paint'.",
    "parameters": {
      "description": "Arguments for the app launcher tool.",
      "properties": {
        "app_name": {
          "description": "The app's common name, e.g. 'notepad' or 'paint'.",
          "type": "string"
        }
      },
      "required": [
        "app_name"
      ],
      "type": "object"
    }
  },
  "browser": {
    "description": "Open a website or search the web in the user's browser. Use when the user asks to look something up online or open a site, e.g. 'search for how volcanoes work' or 'open wikipedia.org'.",
    "parameters": {
      "description": "Arguments for the browser tool.",
      "properties": {
        "action": {
          "description": "'open_url' opens a website address; 'search' looks something up on the web.",
          "enum": [
            "open_url",
            "search"
          ],
          "type": "string"
        },
        "target": {
          "description": "The URL to open, or the search words.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "target"
      ],
      "type": "object"
    }
  },
  "calculator": {
    "description": "Do math: arithmetic, square roots, rounding. Use whenever the user asks a math question, e.g. 'what is 12 times 9' or 'square root of 144'.",
    "parameters": {
      "description": "Arguments for the calculator tool.",
      "properties": {
        "expression": {
          "description": "The math expression to evaluate, e.g. '12 * 9'.",
          "type": "string"
        }
      },
      "required": [
        "expression"
      ],
      "type": "object"
    }
  },
  "desktop_organizer": {
    "description": "Tidy up the user's Desktop by moving files into category folders (Pictures, Documents, Videos, Music, Archives, Other). Use when the user asks to clean up or organize their desktop. 'undo' puts everything back.",
    "parameters": {
      "description": "Arguments for the desktop organizer tool.",
      "properties": {
        "mode": {
          "description": "'organize' tidies the Desktop into folders (the user is always asked first); 'preview' only shows what would move; 'undo' puts the last tidy-up back.",
          "enum": [
            "preview",
            "organize",
            "undo"
          ],
          "type": "string"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    }
  },
  "file_opener": {
    "description": "Open a specific file with its default app, using the folder and name from a file_search result. Use this right after file_search when the user confirms they want that file opened, e.g. 'yes, open it' \u2014 never app_launcher, which only opens a blank app with nothing loaded.",
    "parameters": {
      "description": "Arguments for the file opener tool.",
      "properties": {
        "folder": {
          "description": "The file's folder, from a file_search result.",
          "type": "string"
        },
        "name": {
          "description": "The file's name, from a file_search result.",
          "type": "string"
        }
      },
      "required": [
        "folder",
        "name"
      ],
      "type": "object"
    }
  },
  "file_search": {
    "description": "Find files by name in the user's Documents, Desktop, Downloads or Pictures. Use when the user asks where a file is, e.g. 'find my dragon drawing' or 'where is my homework'. It only looks \u2014 it never changes anything.",
    "parameters": {
      "description": "Arguments for the file search tool.",
      "properties": {
        "file_type": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Extension like 'png', or one of 'image', 'doc', 'video'. Null = any."
        },
        "location": {
          "default": "all",
          "description": "Which folder to search: documents, desktop, downloads, pictures, or all.",
          "enum": [
            "documents",
            "desktop",
            "downloads",
            "pictures",
            "all"
          ],
          "type": "string"
        },
        "query": {
          "description": "Words from the file's name, e.g. 'dragon drawing'.",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  "memory_tool": {
    "description": "Remember a fact about the user, or recall one. Use 'store' when the user asks you to remember something ('remember my favorite color is blue'), and 'recall' when they ask what you remember ('what's my favorite color?').",
    "parameters": {
      "description": "Arguments for the memory tool.",
      "properties": {
        "action": {
          "description": "'store' saves a fact; 'recall' looks one up.",
          "enum": [
            "store",
            "recall"
          ],
          "type": "string"
        },
        "content": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For 'store': the fact, in third person, e.g. 'Favorite color is blue'."
        },
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "For 'recall': words to look up, e.g. 'favorite color'."
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  "weather": {
    "description": "Get the current weather and a short forecast for a city. Use when the user asks about weather, temperature, rain or snow, e.g. 'what's the weather in Lagos' or 'will it rain tomorrow'.",
    "parameters": {
      "description": "Arguments for the weather tool.",
      "properties": {
        "city": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "City name; null means the user's default city."
        },
        "days": {
          "default": 1,
          "description": "How many days of forecast (1-3).",
          "maximum": 3,
          "minimum": 1,
          "type": "integer"
        }
      },
      "type": "object"
    }
  }
}